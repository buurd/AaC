# KNOWN BUGS

## ProductManagerServer shall not syncronize to WebShop before the user pressed the sync-button.
Now it syncs when the productManager saves the product.

## Ordermanager doesn't confim the order. 
The order is auto-confirmed so the ordermanager don't get a chance to confirm it. 