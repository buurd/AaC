apiVersion: policy/v1
kind: RequirementSet
metadata:
  name: webshop-stock-display
  version: "1.0"
spec:
  requirements:
    - id: REQ-041
      type: functional
      c4_level: component
      description: "The Webshop shall store and display the current stock quantity for each product."
      priority: high
      tracesTo: "REQ-001"
      functional_validation:
        test_file: "e2e-tests/cypress/e2e/warehouse_delivery.cy.js"