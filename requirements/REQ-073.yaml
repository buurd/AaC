apiVersion: policy/v1
kind: RequirementSet
metadata:
  name: warehouse-variant-visibility
  version: "1.0"
spec:
  requirements:
    - id: REQ-073
      type: functional
      c4_level: component
      description: "The Warehouse Service shall display the full variant name (including attributes) for items in a delivery."
      priority: medium
      tracesTo: "REQ-036"
      functional_validation:
        test_file: "e2e-tests/cypress/e2e/warehouse_delivery.cy.js"
