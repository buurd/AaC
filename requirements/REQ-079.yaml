apiVersion: policy/v1
kind: RequirementSet
metadata:
  name: production-environment-persistence
  version: "1.0"
spec:
  requirements:
    - id: REQ-079
      type: architectural
      c4_level: deployment
      description: "All stateful services (Databases) shall use persistent storage volumes (PVCs) in the production environment."
      priority: critical
      tracesTo: REQ-078
      validation_policy: "policies/check_traceability.rego"
