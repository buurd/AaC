apiVersion: policy/v1
kind: RequirementSet
metadata:
  name: functional-product-edit
  version: "1.0"
spec:
  requirements:
    - id: REQ-022
      type: functional
      c4_level: component
      description: "When a Product Manager submits the edit product form, the product details shall be updated in the database."
      priority: critical
      tracesTo: "REQ-017"
      functional_validation:
        test_file: "e2e-tests/cypress/e2e/product_crud.cy.js"