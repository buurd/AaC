apiVersion: policy/v1
kind: RequirementSet
metadata:
  name: container-webserver
  version: "1.0"
spec:
  requirements:
    - id: REQ-003
      type: architectural
      c4_level: container
      description: "The Webshop system shall have a Web Server container."
      priority: high
      tracesTo: "REQ-001"
      validation_policy: "policies/check_containers.rego"
      model_validation:
        parent: "Webshop"
        container: "Webshop WebServer"