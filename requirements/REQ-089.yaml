apiVersion: policy/v1
kind: RequirementSet
metadata:
  name: contract-stock-update
  version: "1.0"
spec:
  requirements:
    - id: REQ-089
      type: functional
      c4_level: component
      description: "The Stock Update payload shall include pmId and stock quantity."
      priority: high
      tracesTo: "REQ-042"
      validation_policy: "policies/check_code_structure.rego"
      code_validation:
        file_path: "applications/webshop/src/main/java/com/example/webshop/StockSyncController.java"
        contains: "stock"
