apiVersion: policy/v1
kind: RequirementSet
metadata:
  name: functional-product-create
  version: "1.0"
spec:
  requirements:
    - id: REQ-021
      type: functional
      c4_level: component
      description: "When a Product Manager submits the create product form, a new product shall be persisted in the database and visible in the product list."
      priority: critical
      tracesTo: "REQ-016"
      # Validation would require a stateful test (POST then GET)
      manual_validation: true