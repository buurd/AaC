apiVersion: policy/v1
kind: RequirementSet
metadata:
  name: functional-order-placement
  version: "1.0"
spec:
  requirements:
    - id: REQ-052
      type: functional
      c4_level: component
      description: "The Order Service shall allow customers to place orders."
      priority: critical
      tracesTo: "REQ-048"
      functional_validation:
        test_file: "e2e-tests/cypress/e2e/order_flow.cy.js"