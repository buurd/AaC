apiVersion: policy/v1
kind: RequirementSet
metadata:
  name: flattening-sync
  version: "1.0"
spec:
  requirements:
    - id: REQ-072
      type: functional
      c4_level: component
      description: "The Product Management System shall sync variants as individual products to downstream systems."
      priority: high
      tracesTo: "REQ-012"
      rationale: "See ADR-0008: Flattening Strategy for Product Variations"
      validation_policy: "policies/check_code_structure.rego"
      code_validation:
        file_path: "applications/productManagementSystem/src/main/java/com/example/productmanagement/ProductService.java"
        contains: "syncToWebshop"